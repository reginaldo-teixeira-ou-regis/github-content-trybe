import app from './app';

app.listen(3000, () => console.log('API ONLINE E RODANDO'));

// type Identifiable = {
//   id: number
// };

// function logaId<Type extends Identifiable>(entidade: Type) {
//   console.log(entidade.id);
// }