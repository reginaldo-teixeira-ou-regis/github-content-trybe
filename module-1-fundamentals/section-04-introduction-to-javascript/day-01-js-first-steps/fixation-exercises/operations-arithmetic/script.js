const base = 5;
let height = 8;
const area = base * height;

console.log(base + ' * ' + height + ' = ' + area);

const perimeter = (base * 2) + (height * 2);

console.log('(' + base + ' * 2) + (' + height + ' * 2) = ' + perimeter);