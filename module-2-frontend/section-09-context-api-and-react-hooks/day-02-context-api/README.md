# Day 2 - Contents: 

* Creating and making available the `Context`; 
* Creating the `context` with the `createContext` function of React; 
* The `createContext` returns an `object` which has two component like properties - `Provider` and `Consumer`; 
* It is possible to consume data from a `Context` using the `useContext` Hook; 
* Reading information from the `Context` with the `useContext` Hook; 
* Writing information in Context; 
* Extracting the `Provider` for a Component; 
