const number1 = 10;
const number2 = 22;
let largerNumber;

if(number1 > number2) {
    largerNumber = "O numero " + number1 + " é o maior entre os dois numeros apresentados.";
} else {
    largerNumber = "O numero " + number2 + " é o maior entre os dois numeros apresentados.";
}
console.log(largerNumber);